span
    -  let flag = false;
        each actor in actors
                if flag
                    span #{', '}
                    a(id=actor.id+salt class="actor-name__text") #{' '+actor.name}  #{actor.surname}
                else
                    a(id=actor.id+salt class="actor-name__text") #{actor.name}  #{actor.surname}
                - flag = true