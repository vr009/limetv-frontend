div(class="profile")
    h1(class="title-page__text") Настройки профиля
    div(id="error")
    div(class="profile-info-settings__row")
        div(class="profile-info-picture__column")
            img(src=avatar id="avatar")
            input(type="submit" name="pic" value="Изменить" id="new_avatar")
        div(class="profile-info-settings__column")
            h3(class="profile-info-login-title__text") Пользователь:
                span(class="profile-info-login-name__text") #{' '+login}
            form(id="profile-info-pass__form")
                div
                    input(type="password" placeholder="Пароль" class="profile-info-pass__input")
                    input(type="submit" class="profile-info-save__btn" name="password" value="Сохранить" style="margin-bottom: 30px;")
            form(id="profile-info-bio__form")
                div
                    textarea(placeholder=" О себе" maxlength="512" rows="12" cols="33" class="profile-info-bio__textarea") #{about}
                    input(type="submit" class="profile-info-save__btn" name="login" value="Сохранить")